export default function getResponseFromAPI() {
  return new Promise(() => {});
}
// getResponseFromAPI() is returning a Promise object, which is an instance of the Promise class.
// it still returns a Promise instance
// When getResponseFromAPI() is called and returns the Promise, the instanceof operator checks whether the returned value is an instance of the Promise class
